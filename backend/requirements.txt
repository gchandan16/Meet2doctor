# FastAPI core
fastapi==0.115.0
uvicorn[standard]==0.30.0   # ASGI server

# Database
sqlalchemy==2.0.32
mysqlclient==2.2.4          # or mysql-connector-python (alternative)
alembic==1.13.2             # DB migrations

# Pydantic (for validation)
pydantic==2.9.2
pydantic-settings==2.5.2    # for config/env handling

# Authentication & Security
python-jose==3.3.0          # JWT
passlib[bcrypt]==1.7.4      # password hashing
python-multipart==0.0.9     # form-data support (file upload/login forms)

# Async Support (optional but useful)
aiomysql==0.2.0             # if you prefer async MySQL

# Utils
requests==2.32.3            # API calls if needed
email-validator==2.2.0      # email validation

# Testing
pytest==8.3.2
httpx==0.27.2               # for async test requests


python-jose[cryptography]
passlib[bcrypt]
